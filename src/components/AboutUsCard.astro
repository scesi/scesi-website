---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  image: ImageMetadata;
  isPictureLast: boolean;
  alt: string;
  content: string;
}

const { image, alt, content, isPictureLast } = Astro.props;
---

<article
  class:list={[
    'flex flex-col-reverse justify-between',
    isPictureLast ? 'md:flex-row' : 'md:flex-row-reverse',
  ]}
>
  <p
    class:list={[
      'm-auto mr-0 text-center font-poppins',
      isPictureLast ? 'md:mr-10' : 'md:ml-10',
    ]}
  >
    {content}
  </p>

  <Image
    class="mx-auto mb-4 h-[250px] w-full max-w-[350px] rounded-lg object-cover md:mb-0 md:min-w-[350px]"
    width={400}
    src={image}
    alt={alt}
  />
</article>
